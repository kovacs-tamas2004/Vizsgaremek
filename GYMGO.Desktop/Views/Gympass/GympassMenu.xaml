<UserControl x:Class="GYMGO.Desktop.Views.Gympass.GympassMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:GYMGO.Desktop.ViewModels.Gympass"
             xmlns:local="clr-namespace:GYMGO.Desktop.Views.Gympass" d:DataContext="{d:DesignInstance Type=viewModels:GympassViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">
    <Border>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button 
            Style="{StaticResource subMenuButton}"
            HorizontalAlignment="Left"
            Width="150"
            Margin="235 0 0 0"
            Content="Bérletek"
            Command="{Binding ShowPassesViewCommand}"
        />
            <Button 
            Style="{StaticResource subMenuButton}"
            HorizontalAlignment="Left"
            Width="150"
            Margin="12 0 0 0"
            Content="Jegyek"
            Command="{Binding ShowTicketsViewCommand}"
        />
        </StackPanel>
    </Border>
</UserControl>
